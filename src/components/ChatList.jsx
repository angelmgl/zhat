
/*            renderChatList={(chatAppProps) => <ChatList {...chatAppProps} />} */

const ChatList = (props) => {
    console.log(props);

    return(
        <div></div>
    )
}

export default ChatList;